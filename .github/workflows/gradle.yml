name: Build and Release Mod

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 17  # or the required version for Minecraft modding
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build with Gradle
        run: ./gradlew build

      - name: Get mod version from gradle.properties
        id: get_version
        run: |
          VERSION=$(grep '^mod_version' gradle.properties | cut -d'=' -f2 | xargs)
          echo "MOD_VERSION=$VERSION" >> $GITHUB_ENV

      - name: Create Release
        id: create-release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: "v${{ env.MOD_VERSION }}"
          release_name: "Expeditive v${{ env.MOD_VERSION }}"
          draft: false
          prerelease: false

      - name: Upload JAR to Release
        uses: actions/upload-release-asset@v1
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: build/libs/Expeditive-${{ env.MOD_VERSION }}.jar
          asset_name: Expeditive-${{ env.MOD_VERSION }}.jar
          asset_content_type: application/java-archive
